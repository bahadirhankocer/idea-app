/**
 * Public URL of the push Worker (see worker/README.md). This is not a secret; it can be committed.
 * Leave empty until the Worker is deployed. It can also be set from a setup link (`#w=`).
 */
export const DEFAULT_PUSH_WORKER_URL = '';
